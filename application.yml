eureka:
   cleint:
      register-with-eureka: true
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8761/eureka/
microservices:
   angular-backend:
      endpoints:
         endpoint:
            uri: http://ANGULAR-BACKEND/authenticate
   movie-catalog:
      endpoints:
         endpoint:
            uri: http://MOVIE-CATALOG/
